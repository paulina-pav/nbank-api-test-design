name: Build image and run tests

on:
  push:
#  workflow_dispatch:

jobs:
  build-and-push:
    name: Build and push test image
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
          cache: maven

      - name: Build project
        run: mvn -B clean package -DskipTests=true

      - name: Run Checkstyle
        run: mvn -B verify -DskipTests=true

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/nbank-tests:latest
          platforms: linux/amd64

  run-tests:
    name: Run API tests in Docker Compose
    needs: build-and-push
    if: ${{ needs.build-and-push.result == 'success' }}
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Run tests with Docker Compose
        working-directory: infra
        run: GITHUB_WORKSPACE=${{ github.workspace }} bash ./run-tests-with-docker-compose.sh

      - name: Upload run logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: run-logs
          path: infra/test-output/**/logs

      - name: Upload surefire report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: surefire-report
          path: infra/test-output/**/report

      - name: Upload allure results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: allure-results
          path: infra/test-output/**/allure-results

      - name: Upload allure html report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: allure-report
          path: infra/test-output/**/allure-report


      - name: Debug Swagger coverage output
        if: always()
        run: |
          pwd
          ls -la target || true
          ls -la target/swagger-coverage-output || true
          find target/swagger-coverage-output -type f || true



##добавили ращрешение на исполнение команды
      - name: Run Swagger coverage
        if: always()
        run: chmod +x .swagger-coverage-commandline/bin/swagger-coverage-commandline && .swagger-coverage-commandline/bin/swagger-coverage-commandline -s http://localhost:4111/v3/api-docs -i target/swagger-coverage-output




#      - name: Load test report history
#        uses: actions/checkout@v2
#        if: always()
#        with:
#          ref: gh-pages
#          path: gh-pages
#
#      - name: Build test report
#        uses: simple-elf/allure-report-action@v1.7
#        if: always()
#        with:
#          gh_pages: gh-pages
#          allure_results: target/allure-results
#          allure_history: allure-history
#
#      - name: Build Swagger coverage report
#        if: always()
#        run: sudo cp swagger-coverage-report.html allure-history/${{ github.run_number }}
#
#      - name: Publish test report
#        uses: peaceiris/actions-gh-pages@v3
#        if: always()
#        with:
#          github_token: ${{ secrets.GITHUB_TOKEN }}
#          publish_branch: gh-pages
#          publish_dir: allure-history
#
      - name: Clean up services
        if: always()
        run: cd infra && docker compose down